[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"
startCommand = "npm start"

[deploy]
startCommand = "SITE_URL=https://koczihamihaly.com node start.mjs"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
